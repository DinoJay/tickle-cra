import React from 'react';
import LeaderBoard from './LeaderBoard';
const FriendsActivity = props => (React.createElement("div", { className: "flex-grow" },
    React.createElement(LeaderBoard, Object.assign({}, props))));
export default FriendsActivity;
